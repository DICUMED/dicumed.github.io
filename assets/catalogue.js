function renderProducts(category='All'){const root=document.getElementById('productGrid');const q=(document.getElementById('search')?.value||'').toLowerCase();const list=window.DICUMED_PRODUCTS.filter(p=>(category==='All'||p.category===category)&&(!q||(`${p.name} ${p.category} ${p.desc}`).toLowerCase().includes(q)));root.innerHTML=list.map(p=>`<article class="productCard"><div class="productImage"><img src="${p.img}" alt="Illustrative icon for ${p.name}"></div><div class="productBody"><div class="tag">${p.category}</div><h3>${p.name}</h3><p>${p.desc}</p><div class="productActions"><button class="btn primary" onclick="addToQuote('${p.id}')">Add to quote</button><a class="btn outline" href="mailto:info@dicumed.co.uk?subject=${encodeURIComponent('DICUMED enquiry: '+p.name)}">Enquire</a></div></div></article>`).join('')||'<div class="empty">No matching products found.</div>'}
function setCategory(cat,el){document.querySelectorAll('.filterBtn').forEach(b=>b.classList.remove('active'));el.classList.add('active');renderProducts(cat)}
document.addEventListener('DOMContentLoaded',()=>renderProducts());
